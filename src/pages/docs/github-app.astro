---
import Layout from '../../layouts/Layout.astro';
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { CheckCircle2, XCircle, RefreshCw, Copy } from "lucide-react";

// In a real scenario, this would check `locals.runtime.env` or similar.
// Since this is build-time static or SSR, we might need an API or check process.env if Node.
// For now, we'll simulate state or fetch from an API client-side if needed.
// This example assumes SSR context availability or default false for "not configured" until proven otherwise.

const WORKER_URL = "https://code-review-bot.hacolby.workers.dev";
const MANIFEST_Sample = {
    url: WORKER_URL,
    hook_url: `${WORKER_URL}/api/webhooks`
};
---

<Layout title="GitHub App Configuration">
    <div class="space-y-6">
        <div>
            <h1 class="text-3xl font-bold tracking-tight">GitHub App Configuration</h1>
            <p class="text-muted-foreground mt-2">Setup code-review-bot to interact with your repositories.</p>
        </div>

        {/* Status Indicator Section */}
        <Card className="border-l-4 border-l-yellow-500 bg-card/50">
            <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
                <CardTitle className="text-xl font-semibold">Configuration Status</CardTitle>
                <Button variant="ghost" size="icon" className="h-8 w-8 text-muted-foreground hover:text-primary">
                    <RefreshCw className="h-5 w-5" />
                </Button>
            </CardHeader>
            <CardContent>
                <div class="flex items-center gap-4 py-4">
                    <div class="flex items-center gap-2 text-yellow-500 font-medium text-lg">
                        <XCircle className="h-6 w-6" />
                        <span>Not Configured / Unknown</span>
                    </div>
                    <p class="text-sm text-muted-foreground">
                        Please follow the instructions below to configure the GitHub App and add secrets to your Worker.
                    </p>
                </div>
            </CardContent>
        </Card>

        {/* Instructions Section */}
        <div class="grid gap-6">
            <h2 class="text-2xl font-bold">Setup Instructions</h2>
            
            <div class="space-y-4">
                <div class="rounded-lg border p-4 bg-card">
                    <h3 class="font-semibold mb-2">1. Create GitHub App</h3>
                    <p class="text-sm text-muted-foreground mb-4">
                        Go to GitHub Developer Settings > GitHub Apps > New GitHub App.
                    </p>
                    
                    <div class="grid gap-4 md:grid-cols-2">
                        <div class="space-y-2">
                            <label class="text-xs font-medium uppercase text-muted-foreground">Homepage URL</label>
                            <div class="flex gap-2">
                                <code class="flex-1 bg-muted p-2 rounded text-sm font-mono">{WORKER_URL}</code>
                                <Button variant="outline" size="icon" className="h-9 w-9">
                                    <Copy className="h-4 w-4" />
                                </Button>
                            </div>
                        </div>

                        <div class="space-y-2">
                            <label class="text-xs font-medium uppercase text-muted-foreground">Webhook URL</label>
                            <div class="flex gap-2">
                                <code class="flex-1 bg-muted p-2 rounded text-sm font-mono">{MANIFEST_Sample.hook_url}</code>
                                <Button variant="outline" size="icon" className="h-9 w-9">
                                    <Copy className="h-4 w-4" />
                                </Button>
                            </div>
                        </div>

                         <div class="space-y-2">
                            <label class="text-xs font-medium uppercase text-muted-foreground">Webhook Secret</label>
                            <div class="flex gap-2">
                                <code class="flex-1 bg-muted p-2 rounded text-sm font-mono">generate-random-secret-here</code>
                                <Button variant="outline" size="icon" className="h-9 w-9">
                                    <Copy className="h-4 w-4" />
                                </Button>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="rounded-lg border p-4 bg-card">
                    <h3 class="font-semibold mb-2">2. Permissions & Events</h3>
                    <ul class="list-disc pl-5 space-y-1 text-sm text-muted-foreground">
                        <li><strong>Repository permissions:</strong> Contents (Read & Write), Pull requests (Read & Write), Issues (Read & Write).</li>
                        <li><strong>Subscribe to events:</strong> Pull request, Issue comment, Push (required for Auto-Pilot).</li>
                    </ul>
                </div>

                <div class="rounded-lg border p-4 bg-card">
                     <h3 class="font-semibold mb-2">3. Worker Secrets</h3>
                     <p class="text-sm text-muted-foreground mb-3">Run these commands locally to set secrets:</p>
                     <div class="bg-muted p-3 rounded font-mono text-sm space-y-2 overflow-x-auto">
                        <p>wrangler secret put GITHUB_APP_ID</p>
                        <p>wrangler secret put GITHUB_PRIVATE_KEY</p>
                        <p>wrangler secret put GITHUB_WEBHOOK_SECRET</p>
                     </div>
                </div>
            </div>
        </div>
    </div>
</Layout>
